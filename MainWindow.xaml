<Window xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Dev2Live Deployer" Height="450" Width="600"
        WindowStartupLocation="CenterScreen" ResizeMode="CanMinimize"
        Background="#1E1E1E" Foreground="#DDDDDD" FontFamily="Segoe UI">

    <Window.Resources>
        <Style TargetType="Label">
            <Setter Property="Foreground" Value="#AAAAAA" />
            <Setter Property="FontSize" Value="12" />
            <Setter Property="Margin" Value="0,5,0,0" />
        </Style>
        <Style TargetType="TextBox">
            <Setter Property="Background" Value="#333333" />
            <Setter Property="Foreground" Value="White" />
            <Setter Property="BorderBrush" Value="#555555" />
            <Setter Property="Padding" Value="5" />
            <Setter Property="Margin" Value="0,0,0,10" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border Background="{TemplateBinding Background}" 
                                BorderBrush="{TemplateBinding BorderBrush}" 
                                BorderThickness="1" CornerRadius="3">
                            <ScrollViewer x:Name="PART_ContentHost"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style TargetType="Button">
            <Setter Property="Background" Value="#007ACC" />
            <Setter Property="Foreground" Value="White" />
            <Setter Property="Padding" Value="10,5" />
            <Setter Property="Cursor" Value="Hand" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="3">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#0090F1"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter Property="Background" Value="#005A9AAA"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/> <!-- Header -->
            <RowDefinition Height="*"/>    <!-- Form -->
            <RowDefinition Height="Auto"/> <!-- Actions -->
            <RowDefinition Height="Auto"/> <!-- Status -->
        </Grid.RowDefinitions>

        <!-- Header -->
        <TextBlock Text="Dev2Live" FontSize="24" FontWeight="Bold" Foreground="White" Margin="0,0,0,20"/>

        <!-- Form -->
        <StackPanel Grid.Row="1">
            <!-- Local Source -->
            <Label Content="LOCAL SOURCE DIRECTORY"/>
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <TextBox x:Name="TxtSourcePath" Grid.Column="0"/>
                <Button x:Name="BtnBrowseSource" Content="..." Grid.Column="1" Width="30" Height="28" Margin="5,0,0,10" Background="#444444"/>
            </Grid>

            <!-- Remote Connection Details -->
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="10"/>
                    <ColumnDefinition Width="*"/>
                </Grid.ColumnDefinitions>
                
                <StackPanel Grid.Column="0">
                    <Label Content="REMOTE HOST (IP/DNS)"/>
                    <TextBox x:Name="TxtRemoteHost"/>
                </StackPanel>

                <StackPanel Grid.Column="2">
                    <Label Content="REMOTE USER"/>
                    <TextBox x:Name="TxtRemoteUser"/>
                </StackPanel>
            </Grid>

            <!-- SSH Key -->
            <Label Content="SSH PRIVATE KEY PATH"/>
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <TextBox x:Name="TxtKeyPath" Grid.Column="0"/>
                <Button x:Name="BtnBrowseKey" Content="..." Grid.Column="1" Width="30" Height="28" Margin="5,0,0,10" Background="#444444"/>
            </Grid>

            <!-- Remote Destination -->
            <Label Content="REMOTE DESTINATION DIRECTORY"/>
            <TextBox x:Name="TxtRemoteDest"/>
        </StackPanel>

        <!-- Actions -->
        <Grid Grid.Row="2" Margin="0,10,0,0">
            <Button x:Name="BtnDeploy" Content="DEPLOY TO LIVE" Height="40" FontSize="14" FontWeight="Bold" Background="#28a745"/>
        </Grid>

        <!-- Status/Log -->
        <TextBox x:Name="TxtLog" Grid.Row="3" Height="80" Margin="0,15,0,0" 
                 Background="#111111" BorderThickness="0" FontFamily="Consolas" FontSize="11" 
                 IsReadOnly="True" TextWrapping="Wrap" VerticalScrollBarVisibility="Auto"
                 Text="Ready..."/>
    </Grid>
</Window>
